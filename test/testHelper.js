require('babel-register')();
// const cleaner = require('knex-cleaner');
// process.env.NODE_ENV = 'test';
// const knex = require('../express_src/db');
//
// beforeEach(function() { cleaner.clean(knex); });
